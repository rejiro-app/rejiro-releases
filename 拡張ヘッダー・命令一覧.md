# Re:JIRO ヘッダー・命令ガイド

対応バージョン: 1.5.0 / 更新日: 2026-09-25

Re:JIROで譜面を作成・編集する方向けに、TJAファイルで使用できるヘッダーと命令を紹介します。曲情報の設定から、画像・動画・歌詞の表示、譜面分岐まで、書式と使用例を確認できます。
拡張機能には、他のTJAプレイヤーでも使用されている形式を含みます。
TJC/TJD、genre.ini、テーマファイル、アプリの設定項目は対象外です。古いバージョンでは利用できない機能があるため、最新版をご利用ください。

## 1. 基本的な書き方

- ヘッダーは `名前:値`。通常は `#START` より前に書きます。
- 譜面内命令は `#命令 引数`。ヘッダーのコロン形式とは区別してください。
- ヘッダー名・命令名は原則として大文字小文字を区別しません。例では大文字に統一しています。
- `//` 以降はコメントとして除去されます。値や歌詞の途中に `//` を含めないでください。
- 画像・動画・歌詞はTJAからの相対パスで指定してください。パス全体を引用符で囲む必要はありません。
- 音符列のカンマは小節の終端です。小節途中の命令は、前後の音符列の間に別行として記述します。

## 2. 対応する拡張ヘッダー

| ヘッダー | 書式例 | 用途・注意 |
|---|---|---|
| `PREIMAGE` | `PREIMAGE:jacket.png` | 選曲・演奏画面用のジャケット画像。 |
| `BGIMAGE` | `BGIMAGE:background.jpg` | 演奏背景用の画像。ジャケットとは別に指定できます。 |
| `BGMOVIE` | `BGMOVIE:movie.mp4` | 演奏中の背景動画。動画の音声はミュートし、音源は `WAVE` を使用します。 |
| `MOVIEOFFSET` | `MOVIEOFFSET:1.5` | 動画の時間補正。単位は秒。正数で遅らせ、負数で先へ進めます。省略時は0。 |
| `LYRICS` | `LYRICS:lyrics.vtt` | 外部歌詞ファイル。WebVTT / LRCに対応します。 |
| `LYRICFILE` | `LYRICFILE:lyrics.lrc` | `LYRICS` の別名。同じ情報へ設定されるため、併記時は後に書いた値になります。 |
| `DEMOEND` | `DEMOEND:60.0` | 選曲画面の試聴終了位置。秒単位の音源位置で、試聴の長さではありません。 |
| `NOTESDESIGNER` | `NOTESDESIGNER:譜面制作者` | 難易度ごとの譜面制作者。`COURSE` ごとに指定できます。後続に未指定の場合は前の値を引き継ぎます。 |
| `REJIRO` | `REJIRO:OFF` | そのTJAのJIRO Modeを明示指定します。`ON` はBPM互換と小節線8本制限を有効にし、`OFF` は両方を無効にします。`1`/`0`、`TRUE`/`FALSE` も使用できます。 |

### `REJIRO` と負のBPM・拍子

`REJIRO` を省略した譜面は、通常はアプリのJIRO Mode設定に従います。ただし譜面内に負の `#BPMCHANGE` または `#MEASURE`（分子か分母）がある場合、**BPM設定を太鼓さん次郎と互換にする**だけをその譜面で自動的にオンにし、通知します。小節線8本制限など、他のJIRO設定は自動変更しません。ユーザーの保存設定も変更されません。

自動オンを使わずに再生したい場合は、`#START` より前に `REJIRO:OFF` を指定してください。`REJIRO:OFF` は負値の自動判定とユーザー設定より優先されます。`REJIRO:ON` は明示的にJIRO Modeを有効にします。

```tja
REJIRO:OFF
BPM:120
#START
#BPMCHANGE -120
#MEASURE -1/4
1111,
#END
```

命令名は `#MEASURE` です。`#MASURE` は対応する命令ではありません。

### 画像の対応形式

ジャケットや背景画像には、**PNG・JPEG・WebP**をおすすめします。

| 形式 | 拡張子 | 用途・注意 |
|---|---|---|
| PNG | `.png` | 透過画像や文字を含む画像に向いています。 |
| JPEG | `.jpg`、`.jpeg`、`.jfif` | 写真や背景画像に向いています。透過には対応しません。 |
| WebP | `.webp` | ファイルサイズを抑えたい場合に適しています。透過画像も使用できます。 |
| GIF / APNG | `.gif`、`.apng` | TJAから画像として参照できます。アニメーションの再生方法は表示箇所によって異なります。 |
| BMP | `.bmp` | 使用できますが、ファイルサイズが大きくなりやすいためPNGなどをおすすめします。 |
| AVIF | `.avif` | 利用環境が対応している場合に使用できます。表示できない場合はPNG・JPEG・WebPへ変換してください。 |
| SVG | `.svg` | ベクター画像を画像として読み込みます。外部ファイルやフォントに依存するSVGは、PNGへ変換すると表示を揃えやすくなります。 |
| ICO | `.ico` | アイコン画像として参照できます。ジャケットや背景にはPNGなどをおすすめします。 |

上記は `PREIMAGE`・`BGIMAGE` などでファイルを直接指定する場合の一覧です。
アプリの画像選択画面は、主にPNG・JPEG・WebPを対象としています。テーマのカスタム画像など、一部の選択画面ではGIFも選べます。
拡張子だけを書き換えても画像形式は変わりません。変換が必要な場合は画像編集ソフトなどで書き出してください。

```tja
PREIMAGE:jacket.webp
BGIMAGE:background.jpg
```

### 動画の対応形式

背景動画には、**MP4（H.264）またはWebM（VP8 / VP9）**をおすすめします。
動画ファイルには「入れ物の形式」と「映像の圧縮形式」があり、同じ拡張子でも圧縮形式によって再生できない場合があります。

| 形式 | 拡張子 | 用途・注意 |
|---|---|---|
| MP4 | `.mp4` | H.264形式の映像を推奨します。HEVC/H.265など、別の圧縮形式の再生は保証していません。 |
| M4V | `.m4v` | MP4互換の内容で、利用環境が対応する圧縮形式の場合に使用できます。 |
| WebM | `.webm` | VP8またはVP9形式の映像を推奨します。 |
| MOV / OGV | `.mov`、`.ogv` | 読み込み対象には含まれますが、再生できない環境があります。配布用にはMP4またはWebMへの変換をおすすめします。 |

AVI・MKV・WMVなどは対応を保証していません。MP4またはWebMへ変換してご利用ください。
動画内の音声は再生されません。演奏の音源は `WAVE` で別途指定してください。

```tja
WAVE:audio.ogg
BGMOVIE:background.mp4
MOVIEOFFSET:0
```

### 画像・動画が表示されない場合

テーマ側で背景ソースを「単色・グラデーション」にしていると、画像・動画は使われません。
画像を使う設定では、再生可能な動画フレームがある間は動画を優先し、それ以外では背景画像やジャケットを使用します。
曲・コース・フォルダ側で画像を個別設定している場合は、そちらの設定も確認してください。

動画は利用環境が対応する形式で用意してください。同じ拡張子でも、動画の圧縮形式によって再生できない場合があります。
例えば `MOVIEOFFSET:1.5` なら、動画の進行開始を1.5秒遅らせます。開始を待っている間も、先頭フレームが表示される場合があります。

### DEMOENDの扱い

```tja
DEMOSTART:30.0
DEMOEND:60.0
```

この例は音源の30〜60秒を試聴区間にします。
`DEMOEND` が未指定・数値として無効・`DEMOSTART` 以下の場合は、曲の終端を使用します。
曲の長さを超える指定は実際の終端までに制限します。
試聴を無効にしている場合は再生されません。演奏本編の終了位置を変更するヘッダーではありません。

## 3. 歌詞命令

### `#LYRIC`

```tja
#LYRIC ここに歌詞を表示
1111,
#LYRIC 次の歌詞
2222,
#LYRIC
0000,
```

- 命令位置で歌詞を切り替え、次の `#LYRIC` まで表示します。
- 引数なしの `#LYRIC` で消去します。
- 分岐内の指定は、現在の判定ルートに対応するものを使用します。
- 歌詞表示設定とテーマの歌詞ウィジェットが有効である必要があります。
- 外部歌詞の読み込みに成功した場合、外部歌詞が `#LYRIC` より優先します。
- `\n` などを特別な改行命令として解釈する機能はありません。

### 外部歌詞の対応範囲

WebVTT例:

```vtt
WEBVTT

00:01.000 --> 00:03.000
最初の歌詞

00:04.000 --> 00:06.000
次の歌詞
```

LRC例:

```lrc
[00:01.00]最初の歌詞
[00:04.00]次の歌詞
[00:06.00]
```

- 外部歌詞は演奏側の時刻で参照します。`MOVIEOFFSET` は歌詞には適用しません。
- WebVTTは開始・終了時刻、複数行テキストを読みます。STYLE/REGIONや装飾タグは再現しません。
- ルビの読み部分 `<rt>` とその他のタグは取り除き、テキストとして表示します。
- LRCは次の行の開始まで表示。最後の歌詞は残るので、消したい時刻に空の歌詞行を追加してください。
- LRCの1行に複数の時刻タグがある書き方に対応します。メタタグや単語単位のカラオケ表示は対応を保証しません。
- 歌詞ファイルは1曲につき1ファイルを指定してください。カンマ区切りで複数指定しても、使用されるのは **先頭の1ファイルだけ** です。
- ファイル名自体のカンマは `\,` と記述します。例: `LYRICS:lyrics\, part1.lrc`。
- 外部歌詞の取得・解析に失敗した場合、外部歌詞は設定されず、TJA内の `#LYRIC` が残ります。

## 4. 表示・スクロールの互換命令

### `#JPOSSCROLL`

```tja
#JPOSSCROLL 0.5 100
#JPOSSCROLL 0.5 100 0
#JPOSSCROLL 0.5 100 1
```

書式: `#JPOSSCROLL 移動時間 移動量 [方向]`

- 引数は **空白区切り**。カンマ区切りの形式は対応していません。
- 移動時間は秒。0を指定すると、ほぼ瞬時に移動します。
- 方向省略時は、移動量の正負をそのまま使用します。
- 方向 `0` は左、`1` は右。方向付きの場合は移動量の絶対値を使用します。
- 移動量は演奏画面の基準座標で指定します。実画面のピクセル数は解像度・拡大率によって変わります。
- 移動量は累積します。複数命令で往復させる場合は逆方向の移動も指定してください。
- 横方向の判定位置移動に対応。縦移動や拡張引数には対応していません。

### `#SENOTECHANGE`

```tja
#SENOTECHANGE 1
1111,
```

次の音符のドンカツ読み（SENotes）の番号を指定します。番号は1から指定します。
大音符・黄色連打・風船・くす玉・連打終端などは、音符種別に対応する専用の固定表示が優先されます。
すべての番号・音符種別について他プレイヤーと同じ見た目になるとは限りません。

### スクロール方式

| 命令 | 用途 |
|---|---|
| `#HBSCROLL` | HBSCROLL方式へ切り替えます。 |
| `#BMSCROLL` | BMSCROLL方式へ切り替えます。 |
| `#NMSCROLL` | 通常スクロール方式へ戻します。 |

`#START` の前にも、譜面内にも記述できます。
アプリ側のスクロール固定・無効化設定によって、譜面の指定どおりの見た目にならない場合があります。

## 5. その他、対応している関連ヘッダー

以下も使用できますが、Re:JIRO専用の拡張としては扱いません。

| ヘッダー | 用途 |
|---|---|
| `TOTAL` | 魂ゲージ計算に使用する値。適用はゲージモード等にも依存します。 |
| `SONGVOL` | 譜面のBGM音量。通常は0〜100で指定します。 |
| `SEVOL` | 譜面のSE音量。通常は0〜100で指定します。 |
| `LEVEL` | 小数を含む難易度を読み取れます。小数の表示はアプリ設定に依存します。 |
| `MAKER` | 作者文字列を読み取ります。譜面制作者の表示には `NOTESDESIGNER` を使用してください。 |

音量にはアプリ側の設定や、TJA音量指定を上書きする設定も影響します。
### 曲・難易度の基本情報

| ヘッダー | 例 | 説明 |
|---|---|---|
| `TITLE` | `TITLE:Sample Song` | 曲名を指定します。 |
| `SUBTITLE` | `SUBTITLE:--Sample Chart` | サブタイトルを指定します。 |
| `WAVE` | `WAVE:audio.ogg` | 演奏に使用する音源ファイルを指定します。 |
| `BPM` | `BPM:120` | 譜面開始時のBPMを指定します。 |
| `OFFSET` | `OFFSET:-0.5` | 音源に対する譜面の時間補正です。秒単位で、正数は譜面を早め、負数は遅らせます。 |
| `DEMOSTART` | `DEMOSTART:30` | 選曲画面で試聴を始める音源位置を秒で指定します。 |
| `COURSE` | `COURSE:Oni` | 難易度を指定します。例: `Easy`、`Normal`、`Hard`、`Oni`、`Edit`。 |
| `SCOREINIT` | `SCOREINIT:1000` | 値を読み取りますが、1.5.0の演奏スコア計算には反映されません。 |
| `SCOREDIFF` | `SCOREDIFF:100` | 値を読み取りますが、1.5.0の演奏スコア計算には反映されません。 |
| `BALLOON` | `BALLOON:5,10` | 風船などを割るために必要な打数を、譜面に登場する順にカンマ区切りで指定します。 |

判定幅や配点の詳細は[判定・スコア仕様](判定・スコア仕様.md)を参照してください。

## 6. 基本命令

### 譜面の開始と終了

| 命令 | 説明 |
|---|---|
| `#START` | 譜面データの開始を示します。この後に音符列や譜面内命令を書きます。 |
| `#END` | 譜面データの終了を示します。 |

```tja
#START
1111,
2222,
#END
```

この例では、最初の小節に面音符、次の小節に縁音符を配置します。音符列の末尾のカンマは省略しないでください。

### BPM・拍子・スクロール・時間の変更

| 命令 | 書式例 | 説明 |
|---|---|---|
| `#BPMCHANGE` | `#BPMCHANGE 180` | その位置からBPMを変更します。音符の間隔や流れる速度に影響します。 |
| `#MEASURE` | `#MEASURE 3/4` | 小節の長さを分数で指定します。通常の `4/4` に対し、`3/4` は4分音符3個分の長さです。小節の先頭に書いてください。 |
| `#SCROLL` | `#SCROLL 1.5` | スクロール倍率を変更します。`1` が通常、`0.5` が半分、`2` が2倍です。音符を叩く時刻そのものは変わりません。 |
| `#DELAY` | `#DELAY 0.5` | その位置から後の譜面時刻を秒単位でずらします。正数は遅らせ、負数は早めます。 |

```tja
#BPMCHANGE 180
#MEASURE 3/4
#SCROLL 1.5
111,
#MEASURE 4/4
#SCROLL 1
2222,
```

`#BPMCHANGE` を小節途中に入れる場合は、音符列を前後に分けて記述します。

```tja
11
#BPMCHANGE 240
11,
```

`#DELAY` の見た目はスクロール方式によって異なり、必ず画面全体が停止するわけではありません。
次郎互換の時刻処理では、DELAYはミリ秒単位で切り捨てられます。例えば `#DELAY -0.0005` は実効0msになります。
負のBPMやSCROLLを使う特殊な演出にも対応していますが、他プレイヤーとの完全な表示一致を保証するものではありません。

### ゴーゴータイムと小節線

| 命令 | 説明 |
|---|---|
| `#GOGOSTART` | その位置からゴーゴータイムを開始します。背景・エフェクトなどの演出はテーマ設定に従います。 |
| `#GOGOEND` | ゴーゴータイムを終了します。 |
| `#BARLINEOFF` | 小節線を非表示にします。音符や小節の長さは変わりません。 |
| `#BARLINEON` | 小節線を再び表示します。 |

```tja
#GOGOSTART
1111,
#BARLINEOFF
2222,
#BARLINEON
#GOGOEND
0000,
```

小節線の切り替えは、小節の先頭に置くと表示対象が分かりやすくなります。

### 譜面分岐

| 命令 | 書式例 | 説明 |
|---|---|---|
| `#SECTION` | `#SECTION` | 分岐判定用の精度・連打数などの集計をリセットします。スコア表示やコンボ数を0にする命令ではありません。 |
| `#BRANCHSTART` | `#BRANCHSTART p,70,90` | 条件に応じた譜面分岐を開始します。引数は「判定種別,玄人の基準値,達人の基準値」です。 |
| `#N` | `#N` | 普通譜面の音符列を開始します。 |
| `#E` | `#E` | 玄人譜面の音符列を開始します。 |
| `#M` | `#M` | 達人譜面の音符列を開始します。 |
| `#BRANCHEND` | `#BRANCHEND` | 分岐区間を終了し、共通の譜面へ戻ります。 |
| `#LEVELHOLD` | `#LEVELHOLD` | 現在の分岐ルートを維持するための指定です。切り替え途中などでは適用条件が異なるため、対象区間で動作を確認してください。 |

判定種別は次の3種類です。

- `p`：演奏精度（％）で判定します。
- `r`：連打数で判定します。次郎互換では黄色連打の打数を使用します。
- `s`：スコアで判定します。現在の次郎互換では累積スコアを使用し、`#SECTION` 直前からの増加分ではありません。

通常は、玄人の基準値未満なら普通、玄人の基準値以上かつ達人の基準値未満なら玄人、達人の基準値以上なら達人に分岐します。

```tja
#SECTION
11112222,
#BRANCHSTART p,70,90
#N
1111,
#E
12121212,
#M
1122112211221122,
#BRANCHEND
1111,
```

この例では、演奏精度70％未満で普通、70％以上90％未満で玄人、90％以上で達人を選び、その後は共通の面音符へ戻ります。
一般的な譜面では各ルートの長さを揃え、`#BRANCHEND` を明示してください。分岐の先読みや上下移動は次郎互換の処理に従うため、表示の切り替えが命令位置より前に始まる場合があります。

## 7. 記述例
```tja
TITLE:Sample Song
SUBTITLE:--Sample Chart
BPM:120
WAVE:audio.ogg
OFFSET:0
DEMOSTART:30
DEMOEND:50
PREIMAGE:jacket.png
BGIMAGE:background.jpg
BGMOVIE:background.mp4
MOVIEOFFSET:0
COURSE:Oni
LEVEL:8
NOTESDESIGNER:Sample Designer

#START
#LYRIC 最初の歌詞
1111,
#LYRIC 次の歌詞
2222,
#LYRIC
0000,
#END
```

外部歌詞へ切り替える場合は、ヘッダーに `LYRICS:lyrics.vtt` を追加してください。
この例の画像・音源・動画ファイルは別途必要です。
