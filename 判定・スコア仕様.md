# Re:JIRO 判定・スコア仕様

対応バージョン: 1.5.0 / 更新日: 2026-09-25

現行実装の判定・スコア・ゲージ・段位条件・譜面分岐の選択規則を記載します。太鼓さん次郎や本家との完全一致を保証するものではありません。

## 判定

通常音符の判定は、入力時刻と音符時刻の差の絶対値で決まります。表の値は音符時刻を中心に、早い側・遅い側の両方へ適用されます。

| 難易度 | 良 | 可 | 入力を音符に対応付ける範囲 |
|---|---:|---:|---:|
| かんたん・ふつう | ±41.708 ms以内 | ±108.442 ms以内 | ±125.125 ms以内 |
| むずかしい・おに・おに裏 | ±25.025 ms以内 | ±75.075 ms以内 | ±108.442 ms以内 |

- 良の範囲を超え、可の範囲内なら可です。可の範囲を超えた入力は、対応付け範囲内であっても不可になります。
- 対応付け範囲の外では、その入力は音符を叩いた扱いになりません。未判定の音符は、遅い側の対応付け範囲を過ぎると不可になります。
- ドン・カッの種類が異なる入力は、その音符の命中として扱いません。大音符の良・可の判定幅は同じ難易度の通常音符と同じです。
- オートプレイは通常音符を良として処理します。連打・風船・くす玉には良・可・不可ではなく打数を記録します。
- 「音符の判定調整」は判定に渡す入力時刻を変えます。「音符の表示調整」は描画位置のみを変えます。

## スコア

スコア計算の基準となる通常音符数 `N` は、演奏対象のドン・カッ・大音符を数えたものです。ダミー音符と連打は含まず、音源終了後の音符も除きます。譜面分岐がある場合は、計数用の代表ルートを使用します。計算時には `N` の下限を1とします。

| 結果 | 基礎点 |
|---|---:|
| 良 | `ceil(1,000,000 / N / 10) × 10` 点 |
| 可 | `floor(良の基礎点 / 2 / 10) × 10` 点 |
| 不可 | 0点 |
| 黄色連打・大連打 | 1打につき100点 |
| 風船・くす玉 | 1打につき100点。指定打数で終了 |

- 大音符も、同じ判定の通常音符と基礎点は同じです。コンボ数とゴーゴータイムは基礎点を増やしません。
- 通常音符の全良がおよそ100万点になるよう、良の基礎点を10点単位で切り上げます。このため、音符数によっては全良の合計が100万点をわずかに超えます。連打点も別に加算されます。
- 表示スコアは `floor(累積基礎点 × スコア倍率)` です。通常スクロールは1倍、スクロールOFF・固定スクロールは0.5倍です。
- TJAの `SCOREINIT` と `SCOREDIFF` は読み取りますが、1.5.0の演奏スコア計算には使用していません。

## ランク

単曲ではSSを先に判定し、届かなかった場合に表示スコアでS+以下を決めます。

| ランク | 条件 |
|---|---|
| SS | 表示スコアが0点を超え、SS基準点以上 |
| S+ | SS未達で950,000点以上 |
| S | SS未達で900,000点以上 |
| A | SS未達で800,000点以上 |
| B | SS未達で700,000点以上 |
| C | SS未達で600,000点以上 |
| D | 上記のどれにも該当しない |

SS基準点は「通常音符をすべて良で叩いた基礎点＋黄色連打を17打/秒、風船・くす玉を必要打数を上限として17打/秒で叩いた想定点」です。各連打の長さから求めた打数は整数へ切り捨てて計算します。

## 叩けた率

- 「叩けた率」はスコアとは別です。既定では `(良＋可) / (良＋可＋不可)`、設定で可を半分として数える場合は `(良＋可×0.5) / (良＋可＋不可)` を使用します。

## ゲージ

### NORMALゲージ

ゲージは0から始まり、通常音符の判定で増減します。連打の打数はゲージを直接増やしません。

| 判定 | 通常時 | ゴーゴータイム中 |
|---|---:|---:|
| 良 | +2単位 | +2.4単位 |
| 可 | +1単位 | +1.2単位 |
| 不可 | -4単位 | -4単位 |

内部値は0から上限の間で保持します。係数は次のように決まり、正の `TOTAL` 指定が有効な場合は難易度別の値を `100 / TOTAL` で上書きします。

| 難易度 | 低いレベルの係数 | 高いレベルの係数 |
|---|---:|---:|
| かんたん | ★3以下: 0.65 | ★4以上: 0.71 |
| ふつう | ★4以下: 0.70 | ★5以上: 0.76 |
| むずかしい | ★6以下: 0.70 | ★7以上: 0.76 |
| おに・おに裏 | ★8以下: 0.70 | ★9以上: 0.76 |

`加重音符数 = max(1, 通常音符数 + floor(ゴーゴー中の通常音符数 × 0.2))`、`必要単位数 = 加重音符数 × 係数 × 2` です。内部ゲージ比率は `clamp(現在単位数 / 必要単位数 × 1.001, 0, 1)` として計算します。

| 難易度 | 内部の合格ライン | 画面上の合格ライン位置 |
|---|---:|---:|
| かんたん | 60% | 62.5% |
| ふつう | 70% | 68.75% |
| むずかしい | 70% | 75% |
| おに・おに裏 | 80% | 75% |

画面上の位置は表示用に変換した値で、合否は内部のゲージ値で判定します。通常ゲージは終了時に内部値が合格ライン以上ならクリアです。譜面分岐でルートが変わると、そのルートの音符数に基づくゲージ計算へ切り替わります。

### HARD・JIROゲージ

どちらも100%から始まり、0%に達するとFAILEDになります。増減値は次のとおりです。

| 判定 | HARD（単曲） | HARD（コース・段位） | JIRO |
|---|---:|---:|---:|
| 良 | +0.16% | +0.04% | +0.16% |
| 可 | 変化なし | 変化なし | +0.04% |
| 不可 | -9.00% | -9.00% | -2.50% |

値は0～100%に収めます。0%でFAILEDになった後は判定・成績を増やしません。終了まで0%より大きければクリアです。コース・段位では曲をまたいでゲージを引き継ぎます。

## JIRO Mode

JIRO Modeは、譜面の時間計算と小節線表示に関する2つの設定をまとめて切り替える機能です。前節の「JIROゲージ」とは別の機能で、JIRO Modeをオンにするだけではゲージ方式は変わりません。

| 設定 | オンの場合 | オフの場合 |
|---|---|---|
| BPM設定を太鼓さん次郎と互換にする | BPM変更・拍子・遅延を含む譜面時刻の計算や丸めを次郎式に寄せる | Re:JIROの通常処理 |
| 小節線を最大8本に制限する | 小節線の同時描画数を最大8本にする | 本数を制限しない |

画面のJIRO Modeボタンは両方の設定をまとめてオン・オフします。片方だけオンの場合は、ボタンを薄い赤色で表示します。設定画面では各項目を個別に確認できます。

### TJAによる指定と自動切り替え

| 指定・条件 | 実際の動作 |
|---|---|
| `REJIRO:ON` | そのTJAではBPM互換と小節線8本制限をともにオンにする |
| `REJIRO:OFF` | そのTJAでは両方をオフにする。負値による自動オンより優先 |
| `REJIRO` 未指定で、負の `#BPMCHANGE` または `#MEASURE` がある | BPM互換だけを自動でオンにする。小節線の設定は変えない |
| 上記に該当しない | 保存済みのアプリ設定に従う |

`REJIRO` は `#START` より前のヘッダーとして指定します。負の `#MEASURE` は分子・分母のどちらかが負の場合を含みます。自動オンは保存済み設定を書き換えず、BPM互換が元からオンなら通知を出しません。強制中は対象の設定をグレーアウトし、編集できません。

BPM互換と小節線8本制限がともに有効な状態では、黄色連打の途中で `#SCROLL` が変化して連打が伸縮する場合、「短い黄色連打を丸1つで描く」を適用しません。譜面分岐の次郎互換処理は、JIRO ModeのBPM設定とは独立しています。

## 段位の合格条件

段位には赤合格と金合格の目標値があります。金の値を省略した条件は赤と同じ値として扱います。すべての赤条件を満たすと赤合格、すべての金条件も満たすと金合格です。赤条件を1つでも満たせなければ不合格です。金合格に加えて全良（可・不可ともに0）の場合は虹合格として表示します。

| 条件 | 判定 | 設定対象 |
|---|---|---|
| 良 | 指定数以上 | 総合・単曲 |
| 可・不可 | 指定数未満 | 可は総合・単曲、不可は総合 |
| 連打数 | 指定打数以上 | 総合・単曲 |
| 叩けた数・最大コンボ・スコア | 指定値以上 | 総合 |
| 魂ゲージ | 指定割合以上 | 総合（NORMALゲージ時） |
| HARD・JIROゲージ | 0%にせず完走 | ゲージ方式として選択 |

総合は全曲の成績、単曲は指定した曲の成績を使います。「叩けた数」は良・可になった通常音符と連打系の各打を数え、不可や空振りは含めません。最大コンボはコースを通して続くコンボの最大値です。

- 良・可・不可・最大コンボは、総音符数に対する割合でも指定できます。割合は `ceil(対象音符数 × 指定% / 100)` で具体的な数値へ換算します。不可の換算値は最低1です。
- 連打数は打数の固定値か秒速で指定できます。秒速では、黄色連打の有効時間の合計に指定打/秒を掛けて切り上げます。風船・くす玉は各連打の時間から求めた打数を切り捨て、譜面で指定された必要打数を上限として加算します。
- 連打条件が0なら、その条件のゲージを無効表示にします。伏せたランダム課題に叩ける連打がない場合も、その曲の単曲連打条件を無効化して通知します。
- 演奏中に可・不可の上限を超えた場合や、残りの音符・連打では必要数に届かないことが確定した場合は「達成失敗」を表示します。段位の演奏は、その曲の終了時に打ち切ります。
- 未来の単曲条件は、その曲を演奏するまで未確定です。途中終了で未プレイとなった曲は、リザルトで灰色表示します。

## 譜面分岐の選択規則

`#BRANCHSTART 種別,玄人の基準値,達人の基準値` で条件を指定します。条件値が達人の基準値以上ならM（達人）、そうでなく玄人の基準値以上ならE（玄人）、どちらにも届かなければN（普通）を選びます。

| 種別 | 比較する値 | 現行の次郎互換処理 |
|---|---|---|
| `p` | 演奏精度（%） | 良を2点、可を1点、不可を0点として通常音符1個あたり2点で割る |
| `r` | 連打数 | 黄色連打・大連打の打数を使う |
| `s` | スコア | その時点までの累積基礎点を使う |

- `#SECTION` は分岐用の精度と黄色連打数をリセットします。表示スコア・コンボ・ゲージはリセットしません。次郎互換の `s` 判定は区間差分ではなく累積スコアです。
- `#N`・`#E`・`#M` はそれぞれのルートの音符列を定義します。判定に使うのは選ばれたルートの音符です。`#BRANCHEND` 以降は共通譜面へ戻ります。
- `#LEVELHOLD` は条件が合うと現在のルートを維持します。閾値によっては最初からルートが固定される場合もあります。
- 分岐の候補表示と実際のルート適用には時間差があり、表示が命令位置より先に切り替わることがあります。

分岐の次郎互換処理は、JIRO Modeの「BPM設定を太鼓さん次郎と互換にする」とは別の仕組みです。旧分岐処理を有効にした場合は、`r` 判定に連打系の打数、`s` 判定に区間のスコア増分を使用します。
