# Re:JIRO 判定・スコア仕様

対応バージョン: 1.5.0 / 更新日: 2026-09-25

現行実装の通常演奏における判定とスコアを記載します。ゲージ・段位条件・譜面分岐の選択規則は、この文書の対象外です。太鼓さん次郎や太鼓の達人との完全一致を保証するものではありません。

## 判定

通常音符の判定は、入力時刻と音符時刻の差の絶対値で決まります。表の値は音符時刻を中心に、早い側・遅い側の両方へ適用されます。

| 難易度 | 良 | 可 | 入力を音符に対応付ける範囲 |
|---|---:|---:|---:|
| かんたん・ふつう | ±41.708 ms以内 | ±108.442 ms以内 | ±125.125 ms以内 |
| むずかしい・おに・おに裏 | ±25.025 ms以内 | ±75.075 ms以内 | ±108.442 ms以内 |

- 良の範囲を超え、可の範囲内なら可です。可の範囲を超えた入力は、対応付け範囲内であっても不可になります。
- 対応付け範囲の外では、その入力は音符を叩いた扱いになりません。未判定の音符は、遅い側の対応付け範囲を過ぎると不可になります。
- ドン・カッの種類が異なる入力は、その音符の命中として扱いません。大音符の良・可の判定幅は同じ難易度の通常音符と同じです。
- オートプレイは通常音符を良として処理します。連打・風船・くす玉には良・可・不可ではなく打数を記録します。
- 「音符の判定調整」は判定に渡す入力時刻を変えます。「音符の表示調整」は描画位置のみを変えます。

## スコア

スコア計算の基準となる通常音符数 `N` は、演奏対象のドン・カッ・大音符を数えたものです。ダミー音符と連打は含まず、音源終了後の音符も除きます。譜面分岐がある場合は、計数用の代表ルートを使用します。計算時には `N` の下限を1とします。

| 結果 | 基礎点 |
|---|---:|
| 良 | `ceil(1,000,000 / N / 10) × 10` 点 |
| 可 | `floor(良の基礎点 / 2 / 10) × 10` 点 |
| 不可 | 0点 |
| 黄色連打・大連打 | 1打につき100点 |
| 風船・くす玉 | 1打につき100点。指定打数で終了 |

- 大音符も、同じ判定の通常音符と基礎点は同じです。コンボ数とゴーゴータイムは基礎点を増やしません。
- 通常音符の全良がおよそ100万点になるよう、良の基礎点を10点単位で切り上げます。このため、音符数によっては全良の合計が100万点をわずかに超えます。連打点も別に加算されます。
- 表示スコアは `floor(累積基礎点 × スコア倍率)` です。通常スクロールは1倍、スクロールOFF・固定スクロールは0.5倍です。
- TJAの `SCOREINIT` と `SCOREDIFF` は読み取りますが、1.5.0の演奏スコア計算には使用していません。

## ランクと叩けた率

- 単曲のSSランクは、「通常音符をすべて良で叩いた基礎点＋黄色連打を17打/秒、風船・くす玉を必要打数を上限として17打/秒で叩いた想定点」に表示スコアが達した場合に付きます。想定点は、各連打の長さから求めた打数を整数へ切り捨てて計算します。
- SSに届かない場合は、表示スコア95万点以上がS+、90万点以上がS、80万点以上がA、70万点以上がB、60万点以上がC、それ未満がDです。
- 「叩けた率」はスコアとは別です。既定では `(良＋可) / (良＋可＋不可)`、設定で可を半分として数える場合は `(良＋可×0.5) / (良＋可＋不可)` を使用します。
